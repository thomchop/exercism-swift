name: SwiftLint

on: pull_request

jobs:
  lint:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run SwiftLint
        uses: norio-nomura/action-swiftlint@3.1.0
